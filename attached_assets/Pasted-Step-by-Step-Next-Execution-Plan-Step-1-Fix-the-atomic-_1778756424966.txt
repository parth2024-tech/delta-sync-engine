Step-by-Step Next Execution Plan
Step 1: Fix the atomic allocation — do this first, nothing else matters until this is safe.
Write a Postgres function allocate_patient(p_patient_id uuid) that does the resource selection, update, and allocation log insert inside a single transaction. Call it via supabase.rpc('allocate_patient', { p_patient_id }) from the frontend. Remove the loop-of-updates from patients.tsx entirely. Add a UNIQUE constraint on allocations(patient_id, resource_id) as a safety net. Also add an optimistic lock: check that patient.status = 'waiting' inside the function before proceeding and raise an exception if not.
Step 2: Fix the role enum.
Either add triage, doctor, viewer to the app_role enum in a new migration and update the has_role checks, or remap the frontend to use staff and dispatcher consistently. Pick one and make the DB and the frontend agree completely. Update use-auth.tsx to reflect the actual roles.
Step 3: Add Zod validation to all mutations.
Create a src/lib/validation.ts file with schemas for PatientInsert, ResourceInsert, EmergencyCallInsert. Call .parse() at the top of each submit handler before touching Supabase. Show the user friendly field-level errors instead of raw Supabase error strings. This is a one-day task that prevents garbage data.
Step 4: Wire up the AI Gateway for severity.
In the PatientIntakeDialog, after the user clicks "Admit", call the ai-gateway.ts function with the vitals and symptoms text. Show a loading state ("Checking AI score…"). When the result comes back, merge it with the NEWS2 score (weighted average, or take the max). If the AI call fails or times out after 3 seconds, fall back to the deterministic score silently. Store both scores separately in the patients table so you have an audit trail.
Step 5: Build the doctors table and tie it to allocation.
Add a migration for doctors(id, profile_id, hospital_id, specialty, on_shift, current_load, max_load). Add a /staff route. Update the allocate_patient Postgres function from Step 1 to also select an available on-shift doctor with matching specialty and current_load < max_load, increment their load on allocation, and decrement on discharge. Add doctor assignment display to the patient card.
Step 6: Scope realtime channels by hospital.
Change the channel subscriptions in patients.tsx, resources.tsx, and dispatch.tsx to use a filter like filter: \hospital_id=eq.${hospitalId}`in the.on()` call. This cuts down fanout and removes the cross-hospital data exposure.
Step 7: Build the analytics page.
Add time-bucket queries for: average wait-to-allocation time by severity band (group waiting patients by created_at hour), resource utilization over the last 7 days (resources in_use vs total at each hour), and ambulance response times (assigned_at minus created_at on emergency_calls). Use Recharts BarChart and LineChart. This is the page hospital administrators will actually look at every morning.
Step 8: Add an ambulance position simulator.
Write a simple setInterval in dispatch.tsx (or a Supabase Edge Function) that, for ambulances with status enroute_pickup or enroute_hospital, advances their current_node_id one step along route_path every 30 seconds and updates eta_minutes accordingly. When current_node_id reaches the destination node, automatically flip status to the next stage. This makes the map feel live without needing real GPS.
Step 9: Add input validation and edge-case hardening.
Handle: what happens when a red-severity patient arrives and there are zero ICU beds (show a specific warning with the nearest hospital that has capacity, not just an error toast). Handle: what happens when all ambulances are busy (show estimated time until next one is free based on active ETAs). Add confirmation dialogs before discharge. Rate-limit the "Allocate" button to prevent double-clicks during the async window before Step 1's atomic RPC is in place.
Step 10: Seed data, mobile pass, demo prep.
Verify the seed migration creates enough data for a compelling demo: 2 hospitals, 1 with near-full capacity and 1 with space, 3 ambulances, 5 doctors with different specialties, 8 patients in various states. Test the full flow on a mobile viewport — the triage form and dispatch console are the two most important mobile screens. Fix any overflow or tap-target issues.

Priority Order Summary
Fix atomic allocation first — data integrity. Then roles — auth correctness. Then Zod — data quality. Then AI gateway — the differentiating feature. Then doctors — the missing half of the allocation engine. Then analytics — the thing stakeholders will actually judge. Everything else is polish. Steps 1 through 5 are non-negotiable before showing this to anyone real.