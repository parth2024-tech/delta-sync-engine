hase 1: Database Normalization & Object Storage MigrationWe must achieve strict BCNF normalization and stop storing data in Postgres.Drop data from Postgres: Remove data from the blocks table entirely.True Content Addressing: Create a system where the physical data lives in an S3-compatible Object Store (AWS S3, Cloudflare R2), keyed only by its strongHash (SHA-256).Schema Refactor:physical_blocks (S3 bucket): Key = sha256_hash, Value = binary_data.file_versions (Postgres): id, file_id, version_no. (Add a UNIQUE(file_id, version_no) index).version_blocks (Postgres): version_id, block_index, strong_hash.Result: When a block is copied, you simply insert a new version_blocks row pointing to the existing strong_hash. Storage complexity for a 1-byte change drops from $O(N)$ to $O(1)$.Phase 2: Streaming Network Protocol (Killing Base64)JSON is the wrong protocol for transmitting large binary deltas. Base64 adds 33% network overhead and massive CPU parsing overhead.Switch to Custom Binary Protocol or Multipart: Modify upload.ts to accept multipart/form-data.Metadata vs. Payload: Send the operations array (ops) as a JSON string in one form field, and append the raw binary bytes of the literal runs as standard file streams in the same request.Stream parsing: Use a library like busboy or standard Web Streams to parse the incoming literals directly to disk or S3 without holding them in RAM.Phase 3: The $O(1)$ Memory Download PipelineRewrite download.ts to utilize Node.js/Web standard Streams.Query the DB for the ordered list of strongHash identifiers for the requested version.Create a ReadableStream.Iterate through the hashes. For each hash, open a stream from S3 (e.g., s3Client.getObject().Body) and pipe it directly to the outgoing HTTP Response.Result: Memory consumption drops to the size of the stream buffer (usually 64KB), regardless of whether the file is 1MB or 100GB.Phase 4: Chunked Delta ComputationRewrite delta.ts to handle massive files.Change the signature to: async function* computeDeltaStream(fileStream: ReadableStream, oldSignatures: BlockSignature[])Maintain the rolling window in a small, fixed-size ring buffer (e.g., Uint8Array(blockSize * 2)).As chunks arrive from the fileStream, slide the Adler-32 hash across the ring buffer. yield the DeltaOp objects asynchronously.This allows the client CLI to read a 50GB file from disk, compute the delta, and stream the literal bytes over the network using essentially $O(1)$ memory.Phase 5: Concurrency & Lock ManagementFix the race condition in upload.ts.Enforce DB Constraints: Add a unique index in schema.ts: uniqueIndex("file_versions_unique").on(t.fileId, t.versionNo).Optimistic Concurrency: If tx.insert(fileVersions) fails due to a unique constraint violation, catch the error, abort the transaction, and return a 409 Conflict to the client, forcing them to pull the latest version and recompute the delta.